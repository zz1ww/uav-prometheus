# 比赛技术报告

## 文件清单

| 文件 | 说明 |
|---|---|
| `技术报告_终版.docx` | 技术报告**最完整版本**（可编辑） |
| `技术报告_提交版.pdf` | 提交用版本（含图片，22 页） |

> 原项目文件夹里有**三份 docx 迭代版本**，本仓库只保留了最完整的那份：
>
> | 原文件 | 段落数 | 处理 |
> |---|---|---|
> | `微型无人机报告.docx` | 251 | 未保留（最早版本） |
> | `无人机报告.docx` | 279 | 未保留（中间版本） |
> | `无人机报告(2).docx` | 303 | **→ 技术报告_终版.docx**（最完整：有目录页码、图号规范、补充照片位） |
> | `微型无人机报告.pdf` | 22 页 | **→ 技术报告_提交版.pdf** |

---

## 报告结构

| 章节 | 内容 |
|---|---|
| 1. 无人机参数 | 重量/轴距/飞控/传感器/动力系统/投放装置/续航测试 |
| 2. 比赛场地及道具坐标 | 9m×6m×3m 场地描述、道具坐标 |
| 3. 任务分析 | 7 个任务的任务描述、实现方案、关键参数、失败处理 |
| 4. 技术路线 | 系统架构、定位与控制、视觉识别、任务执行策略、软件设计、性能优化、安全防护 |
| 5. 团队分工 | 3 名成员分工 |
| 6. 技术展望 | 扩展应用方向、技术优化方向 |
| 7. 附录 | 飞控源代码说明、照片索引、符合性说明 |

---

## 关键设计（报告中的亮点）

### 以二维码为信息源的任务链

报告第 3.2 节的设计值得注意：**二维码不只是"识别题"，而是整条任务链的信息源**。

```
识别二维码 → 解析出「类别1, 类别2, 降落方向」
                │
                ├─▶ 任务四：决定投放哪个图片靶（类别匹配）
                ├─▶ 任务五：配合特殊靶
                └─▶ 任务七：决定降落方向（left / right）
```

任务之间**不是简单串行，而是有数据依赖**。这是本系统架构上较有想法的一点。

### 视觉与控制的关键桥梁

报告 3.4 节提到一项关键改造：

> 修改开源项目 **SpireCV** 的椭圆检测代码，使其**发布图片靶圆框信息的 ROS 话题**，再编写程序接收图片与圆框信息进行裁剪，用 YOLOv5 分类。

原版 SpireCV 只做检测**可视化**，不对外提供目标位置接口。改造它对外发布圆心坐标，才使"视觉闭环对准"成为可能 —— 这是视觉模块与飞行控制之间的**关键桥梁**。

### 视觉闭环的通用范式

多个任务（投放、穿环、降落）采用了同一套范式：

```
检测目标 → 获取圆心像素坐标 → 判断是否在画面中心
    │                              │
    └──────── 不在 ──▶ 调整位置 ◀──┘
                    在 → 执行动作（下降投放 / 直穿 / 降落）
```

---

## 客观评价（整理者注）

这份报告**工程数据扎实**，但也存在以下问题：

### 优点

- **量化数据充分**：1850g 实测重量、330mm 卡尺测量、10 次飞行 9 次成功、定位误差 <5cm、悬停续航 8分30秒 —— 有测试轮次、有对比数据。
- **任务参数明确**：每个任务都给出了关键参数（高度、速度、半径、超时）和失败处理策略。
- **安全机制完整**：硬件（桨叶保护、遥控接管、急停）+ 软件（定位/通信/姿态监测、超时跳过、电子围栏）双保险。

### 需要注意的问题

| 问题 | 说明 |
|---|---|
| **团队分工是模板化描述** | 报告中写的是"姓名1（队长）""姓名2""姓名3"，未填真实姓名 |
| **分工描述与实际实现有出入** | 分工提到"A* 全局路径规划""TEB 局部轨迹优化""TensorRT 加速部署"，但 `src/` 中实际代码是**固定航点 + 视觉判断**，未体现 A*/TEB 实现 |
| **部分描述偏理想化** | 如"连续多帧确认机制""电子围栏自动减速返航"，代码中未见对应实现 |
| **重量合规性** | 1.1 节称 1850g "符合规则重量要求"，附录 D 又称"控制在 2500 克以内"，两处口径需统一 |

> **建议**：如需在个人作品集中使用本报告，应如实说明自己负责的模块，避免引用未实现的功能（A*/TEB/TensorRT）作为个人成果。

---

## 与代码的对应关系

报告中描述的任务流程与 `src/mission_with_vision.py` 一致：

| 报告任务 | 代码阶段标志 |
|---|---|
| 任务一 携带物块起飞 | `cmd_pub_flag` |
| 任务二 识别二维码 | `getQRcode_done` |
| 任务四 图片靶投放 | `getimg1_done` ~ `getimg4_done` |
| 任务五 特殊靶投放 | `getspT_done` |
| 任务六 穿越圆环 | `crosscir_done` |
| 任务七 定向降落 | `return0_done` / `return1_done` |

> 注意：报告中的"任务三 顺时针绕障碍物飞行"在 `src/` 的主程序里**未见对应实现**（代码中定义了 `get_circular_property` 画圆函数和 `circle_done` 标志，但主流程中未调用）。这可能是因为绕障逻辑在另一份未归档的代码中，或该功能最终未接入主流程。
